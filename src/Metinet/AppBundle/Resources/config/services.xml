<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <!--
    <services>
        <service id="metinet_app.example" class="Metinet\AppBundle\Example">
            <argument type="service" id="service_id" />
            <argument>plain_value</argument>
            <argument>%parameter_name%</argument>
        </service>
    </services>
    -->

    <services>

        <service id="metinet_app.in_memory_fact" class="Metinet\AppBundle\Repository\InMemoryFactRepository" />

        <service id="metinet_app.doctrine_fact" class="Metinet\AppBundle\Repository\DoctrineFactRepository">
            <argument type="service" id="doctrine.orm.entity_manager"></argument>
        </service>

        <service id="user_repository.doctrine" class="Metinet\AppBundle\Repository\UserRepository">
            <argument type="service" id="doctrine.orm.entity_manager"/>
        </service>

        <service id="user_repository" alias="user_repository.doctrine"/>

    </services>

</container>
